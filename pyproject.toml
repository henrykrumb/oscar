[project]
name = "oscar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.4.2",
    "mypy>=2.1.0",
    "ruff>=0.15.20",
]

[project.scripts]
oscar = "oscar.main:cli"
